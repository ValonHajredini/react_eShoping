import React from 'react';
import cssStyle from './ContactHeader.css';
const ContactHeader = (props) => {
    return (
        <div className={cssStyle.HeaderTop}>
        </div>

    );
};
export default ContactHeader;